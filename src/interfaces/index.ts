// You can include shared typings/types in a separate file
// and then use them in any component by importing them. For
// example, to import the interface below do:
//
// import User from 'path/to/typings';

export type User = {
  id: number;
  name: string;
};
